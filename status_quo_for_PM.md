到目前为止，这个 POC 已经从“一个 AI 判断想法”变成了一个可以演示、可以复盘、可以批量验证的产品原型。

它目前验证的是：在企业微信外部群里，机器人能不能根据聊天上下文，判断什么时候该介入、什么时候不该打扰、该说什么，以及是否应该抛出一个信息收集表单。

1. 单条会话分析

产品方可以选择一份模拟企业微信群聊记录，然后拖动到任意一条消息位置，查看“如果聊天进行到这里，AI 会怎么判断”。

每个判断都会展示：

是否建议机器人发言
建议动作类型：不介入、轻量引导、发送表单
AI 为什么这么判断
它参考了哪些聊天消息
置信度
一组可解释评分
如果要发言，候选话术是什么
如果要发表单，对应表单是什么
这让业务方可以逐条复盘 AI 的判断逻辑，而不是只看到一个黑盒结果。

2. 企业微信群聊低保真回放

现在已经有一个更接近企业微信体验的“群聊回放”视图。

它会把样本聊天按企业微信群聊的方式展示出来，用户可以拖动进度，模拟聊天从第 1 条推进到第 N 条。

在每个时间点，右侧会同步展示：

当前这一步 AI 是否建议介入
如果介入，建议发什么
如果适合发表单，会展示一个低保真的表单卡片
如果不介入，会说明原因，比如“信息不足”“已经发过表单”“最新消息来自内部用户”等
这个视图非常适合做产品演示，因为干系人不用理解底层文本格式，也能直观看到机器人在群里的行为节奏。

3. 表单卡片预览

POC 已经不只是生成一段话，还能预览“表单卡片”的样子。

比如在泰国港口照明场景里，当李总明确说可以整理方案资料后，AI 会建议发送“泰国港口项目｜照明解决方案信息提交”的表单卡片。

目前这些表单不是模型随便编的，而是来自预先配置好的表单目录。这样做的好处是：

表单 URL 可控
表单标题和用途可控
不会出现 AI 生成假链接的问题
后续可以很自然地接真实表单系统
4. 批量稳定性报告

除了单个案例演示，现在还可以批量验证 AI 的稳定性。

目前样本集中有 12 个海外基建相关场景，覆盖港口、铁路、能源、水务、物流、园区、桥梁、隧道等主题。系统设置了 17 个关键评估点，用来检查 AI 在不同情境下是否表现稳定。

报告会展示：

总体准确率
正例召回率，也就是该介入的时候有没有介入
误触发率，也就是不该打扰的时候有没有乱发言
动作判断是否正确
表单选择是否正确
候选话术是否包含关键业务信息
哪些案例失败，为什么失败
当前 baseline 是 17/17 通过，整体状态 PASS。

5. 多场景样本库

现在不再只有泰国港口照明一个样本，而是有一组可扩展的模拟群聊样本。

这些样本覆盖了几类重要情况：

正常触发：外部供应商表达能力，项目方释放时间点，AI 建议补充资料
表单触发：供应商明确愿意提交资料，AI 建议发送表单
不应触发：只是寒暄、信息不足、内部用户总结、已经发过表单
边界情况：多个外部供应商同时出现、项目边界不清、不同主题的项目机会
这让 POC 可以验证“可拓展性”，而不是只为一个截图写死逻辑。

6. DeepSeek / Mock 模式切换

产品演示时默认使用 Mock 模式，也就是不依赖真实模型 API，保证演示稳定。

如果配置了 DeepSeek API key，界面里可以切换到 DeepSeek 模式，用真实模型润色话术。没有 key 的时候，系统会自动回退到 Mock，不会因为 API 配置缺失导致演示失败。

这对 POC 很重要：业务演示可以稳定跑，技术验证又保留了接真实模型的入口。

7. Streamlit 演示应用

目前已经有一个完整的 Streamlit 应用，适合部署到 Streamlit Cloud。

它包含三个主要页面：

单点分析
群聊回放
批量稳定性报告
从产品角度看，这已经可以作为第一版演示台使用：既能讲一个具体故事，也能展示批量稳定性，还能让干系人理解“AI 为什么这么做”。

总结一下：目前这个 POC 已经具备了“可看、可解释、可批量验证、可扩展样本、可接真实 LLM”的基础产品能力。它还不是企业微信生产机器人，但已经足够用来验证 AI 侧是否值得继续投入。