到目前为止，这个 POC 已经从“一个 AI 判断想法”推进到了一个比较完整的产品验证原型。

它目前验证的是：在企业微信外部群里，如果后台拿到一份持续追加的聊天记录文本，AI 能不能根据上下文判断什么时候该介入、什么时候不该打扰、该说什么，以及是否应该抛出一个信息收集表单。

现在的完成度可以理解为接近 95%。它还不是生产机器人，但已经足够支持产品评审、业务演示和下一轮技术可行性判断。

1. 单条会话分析

产品方可以选择一份模拟企业微信群聊记录，然后拖动到任意一条消息位置，查看“如果聊天进行到这里，AI 会怎么判断”。

每个判断都会展示：

是否建议机器人发言
建议动作类型：不介入、轻量引导、发送表单
AI 为什么这么判断
它参考了哪些聊天消息
置信度
一组可解释评分
如果要发言，候选话术是什么
如果要发表单，对应表单是什么

这让业务方可以逐条复盘 AI 的判断逻辑，而不是只看到一个黑盒结果。

例如，在泰国港口照明场景里，当项目方释放 7 月启动会等时间点、供应方又表达了方案能力时，AI 会建议先轻量引导对方补充方案资料。等供应方明确表示可以整理资料后，AI 会建议发送配置好的照明方案收集表单。

2. 企业微信群聊低保真回放

现在已经有一个更接近企业微信体验的“群聊回放”视图。

它会把样本聊天按群聊方式展示出来，用户可以拖动进度，模拟聊天从第 1 条推进到第 N 条。

在每个时间点，右侧会同步展示：

当前这一步 AI 是否建议介入
如果介入，建议发什么
如果适合发表单，会展示一个低保真的表单卡片
如果不介入，会说明原因，比如“信息不足”“已经发过表单”“最新消息来自内部用户”等

这个视图非常适合做产品演示。干系人不需要理解底层文本格式，也能直观看到机器人在群里的行为节奏：它不是每句话都抢答，而是在信息缺口和业务机会都比较明确时才介入。

3. 表单卡片预览

POC 已经不只是生成一段话，还能预览“表单卡片”的样子。

比如在泰国港口照明场景里，当李总明确说可以整理方案资料后，AI 会建议发送“泰国港口项目｜照明解决方案信息提交”的表单卡片。

目前这些表单不是模型随便编的，而是来自预先配置好的表单目录。这样做的好处是：

表单 URL 可控
表单标题和用途可控
不会出现 AI 生成假链接的问题
后续可以很自然地接真实表单系统

这也明确了一个重要产品边界：LLM 可以帮忙把话术说得更自然，但不允许自己生成真实表单链接，也不允许绕过表单目录。

4. 批量稳定性报告

除了单个案例演示，现在还可以批量验证 AI 的稳定性。

目前样本集中有 12 个海外基建相关场景，覆盖港口、铁路、能源、水务、物流、园区、桥梁、隧道等主题。系统设置了 17 个关键评估点，用来检查 AI 在不同情境下是否表现稳定。

报告会展示：

总体准确率
正例召回率，也就是该介入的时候有没有介入
误触发率，也就是不该打扰的时候有没有乱发言
动作判断是否正确
表单选择是否正确
候选话术是否包含关键业务信息
哪些案例失败，为什么失败

当前批量稳定性 baseline 是 17/17 通过，整体状态 PASS。

从产品角度看，这个报告回答的是：“这个机器人不是只在一个截图里表现正常，它在一组不同主题、不同长度、不同角色数量的对话里也能保持基本稳定。”

5. 多场景样本库

现在不再只有泰国港口照明一个样本，而是有一组可扩展的模拟群聊样本。

这些样本覆盖了几类重要情况：

正常触发：外部供应商表达能力，项目方释放时间点，AI 建议补充资料
表单触发：供应商明确愿意提交资料，AI 建议发送表单
不应触发：只是寒暄、信息不足、内部用户总结、已经发过表单
边界情况：多个外部供应商同时出现、项目边界不清、不同主题的项目机会

这让 POC 可以验证“可拓展性”，而不是只为一个案例写死逻辑。

后续如果业务方希望切到别的主题，比如海外医疗设备、跨境物流、能源 EPC、海外园区招商，也可以沿用同一套样本和评估方式继续扩展。

6. DeepSeek / Mock 模式切换

产品演示时默认使用 Mock 模式，也就是不依赖真实模型 API，保证演示稳定。

如果配置了 DeepSeek API key，界面里可以切换到 DeepSeek 模式，用真实模型增强话术、语义理解和解释。没有 key 的时候，系统会自动回退到 Mock，不会因为 API 配置缺失导致演示失败。

现在项目也补充了 `.env.example`，本地开发者可以按示例创建自己的 `.env` 文件，填入 DeepSeek 配置。真实 key 不会提交到代码库。

这对 POC 很重要：业务演示可以稳定跑，技术验证又保留了接真实模型的入口。

7. LLM 增强复盘

现在 POC 不只是“能接上 DeepSeek”，而是可以复盘 DeepSeek 到底带来了什么提升。

我们把 LLM 的作用限制在三个方面：

让机器人话术更自然
帮助理解更复杂的聊天语义
把 AI 为什么介入或不介入解释得更像业务复盘，而不是工程日志

为了避免模型每次输出不同导致演示不稳定，系统会把真实 DeepSeek 输出保存成固定快照。之后即使没有 API key，也可以用这些快照做离线复盘。

当前 LLM 复盘集有 10 个关键案例，覆盖正例、负例和边界例。报告会检查：

语义信号是否识别正确
业务解释是否覆盖关键原因
话术是否包含必要业务信息
话术是否保持克制，没有越权承诺或乱发链接

当前 LLM 增强复盘 baseline 是 10/10 通过，整体状态 PASS。

从产品角度看，这意味着我们不仅能说“LLM 可以让话术更好”，还可以拿出一组固定案例说明：它在哪些地方让表达更自然，在哪些地方帮助解释上下文，并且没有突破表单和发送决策的安全边界。

8. Streamlit 演示应用

目前已经有一个完整的 Streamlit 应用，适合部署到 Streamlit Cloud。

它包含四个主要页面：

单点分析
群聊回放
批量稳定性报告
LLM 增强复盘

从产品演示角度看，这四个页面各自承担不同作用：

单点分析适合讲“AI 如何判断某一个时刻”
群聊回放适合讲“机器人在群里的行为节奏”
批量稳定性报告适合讲“规则触发是否稳定”
LLM 增强复盘适合讲“真实模型是否提升了话术和解释质量”

这让 POC 不再只是一个技术 demo，而是一个可以支持业务沟通的演示台。

9. 当前已经能回答的问题

这个 POC 目前已经能比较完整地回答三个关键问题。

第一，AI 是否能判断该不该介入。

目前答案是：在海外基建商机信息收集这个范围内，可以。系统能识别项目机会、供应方能力表达、资料意愿、时间窗口和已发表单等信号，并在不确定时默认不介入。

第二，AI 的判断是否能被业务方复盘。

目前答案是：可以。每次判断都有证据消息、理由、置信度、评分和候选话术，批量报告还能展示哪些场景通过、哪些场景失败。

第三，LLM 是否能在受控边界内带来提升。

目前答案是：可以初步证明。LLM 不负责最终发送决策，也不生成真实表单 URL；它负责增强话术、语义信号和业务解释。通过固定快照，我们可以离线复盘它的输出质量。

10. 当前边界

这个 POC 仍然不是生产机器人。

它目前不处理：

真实企业微信接口
真实表单系统
权限、审计和安全策略
真实客户数据脱敏
生产级监控和报警
多租户配置
真实业务团队的长期运营流程

如果进入下一阶段，重点不应该只是继续加功能，而应该围绕生产化问题展开：真实会话接入、数据安全、人工审核、表单系统、权限边界、客户数据脱敏、线上监控和业务闭环。

总结一下：目前这个 POC 已经具备了“可看、可解释、可批量验证、可扩展样本、可接真实 LLM、可复盘 LLM 增强质量”的产品能力。它还不是企业微信生产机器人，但已经足够支持下一轮业务评审、技术可行性判断，以及是否进入生产化探索的决策。
